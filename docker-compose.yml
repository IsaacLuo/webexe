version: '3.0'
services:
  api:
    build: ./api-server
    ports:
      - "12800:8000"
    working_dir: /app
    command: ['yarn', 'run', 'production']
    restart: always
    volumes:
      - /srv/cailab-database-v2/logs:/app/logs

  frontend:
    build: ./frontend
    ports:
      - "12080:80"
    links:
      - api
    depends_on:
      - api
    command: ['nginx', '-g', 'daemon off;']

    
